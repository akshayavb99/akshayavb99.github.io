{% assign monthly_field_logs = site.field_log | sort: "date" | reverse %}
{% assign timeline_entry_count = 0 %}

<section class="field-log-timeline" aria-labelledby="field-log-timeline-title">
  <h2 id="field-log-timeline-title">Field Log Timeline</h2>

  <div
    class="field-log-timeline__scroll"
    role="region"
    aria-label="Field log entries, newest first"
    tabindex="0"
  >
    <ol class="field-log-timeline__list">
      {% for monthly_log in monthly_field_logs %}
        {% assign daily_entries = monthly_log.entries | sort: "date" | reverse %}
        {% for diary_entry in daily_entries %}
          {% assign timeline_entry_count = timeline_entry_count | plus: 1 %}
          {% assign entry_anchor = diary_entry.date | date: "%Y-%m-%d" %}
          {% assign heading_marker = 'id="' | append: entry_anchor | append: '"' %}
          <li class="field-log-timeline__item">
            <span class="field-log-timeline__marker" aria-hidden="true"></span>
            <article class="field-log-timeline__entry">
              <header>
                <time class="field-log-timeline__date" datetime="{{ diary_entry.date | date_to_xmlschema }}">
                  {{ diary_entry.date | date: "%B %-d, %Y" }}
                </time>
                <h3 class="field-log-timeline__title">
                  <a href="{{ monthly_log.url | relative_url }}#{{ entry_anchor }}">{{ diary_entry.title }}</a>
                </h3>
              </header>

              {% if diary_entry.summary and diary_entry.summary != "" %}
                <div class="field-log-timeline__body">{{ diary_entry.summary | markdownify }}</div>
              {% endif %}

              {% if monthly_log.content contains heading_marker %}
                <a
                  class="field-log-timeline__more"
                  href="{{ monthly_log.url | relative_url }}#{{ entry_anchor }}"
                  aria-label="Read additional content for {{ diary_entry.title }}"
                >...</a>
              {% endif %}

              {% if diary_entry.tags and diary_entry.tags.size > 0 %}
                <ul class="field-log-timeline__tags" aria-label="Tags">
                  {% for tag in diary_entry.tags %}
                    <li class="field-log-timeline__tag">{{ tag }}</li>
                  {% endfor %}
                </ul>
              {% endif %}
            </article>
          </li>
        {% endfor %}
      {% endfor %}
    </ol>

    {% if timeline_entry_count == 0 %}
      <p>No field log entries have been published yet.</p>
    {% endif %}
  </div>
</section>
